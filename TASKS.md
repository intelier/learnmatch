# 클래스 핏 — TASKS

> 한 번에 하나씩. 각 태스크는 30분~2시간 크기.
> 완료 기준은 "눈으로 확인 가능한 상태"로 적는다.

## M1 — 핵심 가설 검증 (설문 → 리포트 → 공유)
- [x] T-01 Next.js 프로젝트 스캐폴드 — 완료 기준: `npm run dev` 후 localhost:3000에 클래스 핏 랜딩 페이지가 뜬다 (기존 HTML은 `legacy/`로 이동, D-03)
- [x] T-02 진단 문항·채점 체계 정의 (D-02: 기존 8문항 → 18문항 확장) — 완료 기준: `lib/questions.ts`에 문항 전체, 채점 함수가 샘플 응답으로 올바른 scores 산출 (`node scripts/check-scoring.ts` 21건 통과)
- [x] T-03 설문 UI (스텝 진행 + 진행바) — 완료 기준: 브라우저에서 전 문항 응답 완료 → 응답·채점 결과가 결과 대기 화면(`/result`)으로 전달됨 (18문항 클릭 검증 완료)
- [x] T-04 리포트 생성 mock 어댑터 + 프롬프트 v1 — 완료 기준: 설문 완료 시 서술형 리포트가 화면에 표시(mock). LLM 교체 지점은 `lib/llm.ts` 하나로 분리 (5개 섹션 리포트 브라우저 검증 완료)
- [x] T-05 LLM API 실연동 (D-05: Gemini 1순위) — 완료 기준: 실제 LLM이 생성한 리포트 표시, 실패 시 폴백 (`gemini-2.5-flash` 리포트 생성·렌더링 검증 완료. 폴백 체인: Gemini → Claude → mock. Claude는 크레딧 충전 시 자동 복귀)
- [x] T-06 공유 링크 v1 — 완료 기준: 리포트 하단 "공유" 버튼 → 고유 URL 복사 → 시크릿 창에서 열람 가능 (무상태 URL 인코딩 `/r/[18자리]`, sessionStorage 없이 열람 검증 완료. M2에서 DB 토큰 방식으로 교체)
- [x] T-07 시드/더미 리포트 예시 — 완료 기준: 랜딩에서 "예시 리포트 보기"로 완성 리포트 1개 열람 가능 (Gemini로 1회 생성 후 `lib/example-report.ts`에 고정 — 열람 시 LLM 호출 없음, 즉시 로딩)
- [x] T-08 Vercel 배포 — 완료 기준: 배포 URL에서 설문→리포트→공유 전 흐름 동작 (learnmatch-zeta.vercel.app — 전 라우트 200, Gemini 리포트 생성, 공유 링크 동작 확인. vercel.json으로 Next.js 프리셋 명시)

## M1.5 — 사용자 피드백 반영 (2026-07-19)
- [x] T-18 리포트 축별 결과 시각화 — 완료 기준: 결과 화면에 5축 레이더 차트 표시 (순수 SVG, 축 라벨+점수 표기)
- [x] T-19 랜딩 참조 이론 탭 — 완료 기준: 랜딩에서 탭(아코디언)을 누르면 SDT·ZPD 등 근거 이론 설명 표시 (5개 탭: SDT/ZPD/학업소진/학습선호/부모관찰)
- [x] T-20 리포트 "의외성" 서술 강화 — 완료 기준: 프롬프트 v2 — "부모님 보시기에는 ~하지만, 실제로는 ~" 형태의 "어쩌면 의외의 모습" 섹션 (Gemini 생성 검증, mock에도 축 조합 기반 구현, 예시 리포트 v2로 재생성)
- [x] T-21 부모 관찰 기반 진단 프레이밍 — 히어로에 "부모님의 관찰을 바탕으로"만 반영. 이후 피드백으로 "자녀 검사 없이" 문구와 부모관찰 설명 탭은 제거 (과한 설명 불필요 판단)

## M2 — 결제 언락
- [x] T-09 Supabase 프로젝트 + 스키마 적용 (D-06: T-08 직후로 앞당김) — 완료 기준: 설문 완료 시 diagnoses/reports 행 생성 확인 + 생성된 리포트 저장(재호출 방지) + 공유 링크를 share_token 방식으로 교체 (로컬 검증 완료 — 행 생성·토큰 링크 2초 서빙·legacy 호환. 프로덕션은 Vercel 환경변수 등록 후 유효)
- [x] T-10 무료 미리보기/유료 전체 게이팅 (D-07: 공감 후킹까지 무료) — 완료 기준: 미결제 상태에서 헤드라인·레이더·한눈에 보기·익숙하시죠만 표시, "의외의 모습"부터 블러+언락 CTA (서버에서 잠금 구간 제거→노출 불가, 언락 시뮬레이션으로 전체 열림 확인. 부수: 프롬프트 v3 헤딩 정리 — Gemini가 헤딩에 지시문 붙이던 문제 해결)
- [x] T-11 Groble 판매 링크 부착 (자리만) — 완료 기준: 언락 CTA → Groble 상품 페이지 이동 (990원/10회권 자리). 교체 지점 lib/groble.ts 하나로 분리, URL 있으면 활성 링크(?ref=share_token)·없으면 "준비 중" 표시. 임시 URL로 링크 활성화 검증 완료. **대기: 실제 상품 등록 후 NEXT_PUBLIC_GROBLE_SINGLE_URL 입력**
- [x] T-12 결제 확인 → 언락 처리 — 완료 기준: 결제 확인된 진단은 전체 리포트 열람 가능 (관리자 언락 API `/api/unlock`, ADMIN_SECRET 인증. 401/404/200 및 언락 후 전체 열람 검증 완료)
  - **보안 판단**: Groble 리다이렉트의 `?ref=` 만으로는 언락하지 않음 (URL만 알면 무료 열람 가능) — 서버가 결제를 확인한 경우에만 언락
  - **대기 1**: `supabase/migrations/003_purchases.sql` 실행 (구매 기록 테이블)
  - **T-12.5 완료**: Groble이 웹훅을 지원해 자동 언락 구현 (`POST /api/webhooks/groble`, HMAC-SHA256 서명 검증). 서명 검증·재시도 멱등성·언락 3가지 시나리오 로컬 검증 완료.
  - **대기**: ① 두 상품에 "구매 시 질문"으로 진단 링크 필수 입력 설정 ② Groble 대시보드에서 웹훅 등록(URL+payment.completed) → 발급된 시크릿을 `GROBLE_WEBHOOK_SECRET`에 저장 (로컬+Vercel)
- [ ] T-13 면책·고지 문구 (U-6 결정 필요) — 완료 기준: 설문 시작·리포트 하단에 고지 노출

## M3 — 성장 루프 + 2단계 준비
- [ ] T-14 공유 페이지 OG 이미지·요약 카드 (U-4 결정 필요) — 완료 기준: 카톡 공유 시 미리보기 카드 표시
- [ ] T-15 재진단·10회권 흐름 — 완료 기준: 10회권 구매자가 추가 진단 시작 가능
- [ ] T-16 데이터 축적 지표 확인 — 완료 기준: 진단 수·결제 수·공유 유입을 쿼리 1개로 확인
- [ ] T-17 학원 DB 스키마 초안 — 완료 기준: PLAN.md에 academies 스키마·수집 계획 추가
