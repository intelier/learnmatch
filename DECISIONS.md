# 클래스 핏 — DECISIONS

> 형식: `## D-NN (날짜) 무엇을 물었나 → 선택 / 이유 / 영향받는 태스크`

## D-01 (2026-07-19) 프로젝트 루트 위치 → `coding/learnmatch` 저장소 사용
- **선택**: 기존 `intelier/learnmatch` 저장소(learnmatch-zeta.vercel.app 배포 중)를 프로젝트 루트로 사용. (인터뷰 없이 기본값으로 결정)
- **이유**: 동일 서비스(ClassFit)의 프로토타입 2개가 이미 이 저장소에 있고 Vercel 배포도 연결되어 있음. 새 저장소를 만들면 배포·이력이 분산됨.
- **영향**: T-01 (스캐폴드 위치), U-7 (기존 HTML 처리)

## D-02 (2026-07-19) 진단 문항·채점 체계 → 기존 8문항 확장
- **선택**: 프로토타입의 SDT+ZPD 채점 축(자기주도성·수준부담·소진·유능감·사회성·학습스타일·깊이/넓이)을 유지하며 15~20문항으로 확장.
- **이유**: 이미 동작하는 검증된 구조를 재사용해 M1 속도 확보. 근거 문헌 정리는 별도 작업으로 분리(→ U-6 면책 문구와 함께 다룸).
- **영향**: T-02 (문항 정의 — 확장 설계 포함), T-04 (리포트 프롬프트가 이 축을 입력으로 사용)

## D-03 (2026-07-19) 기존 HTML 데모 처리 (U-7) → `legacy/` 폴더 보존
- **선택**: `index.html`(학원 매칭 데모)·`learning_diagnostic_full.html`(진단 데모)을 `legacy/`로 이동해 보존. (T-01 승인 시 기본값으로 확정)
- **이유**: T-02에서 문항·채점 로직을 참조해야 하고, 학원 매칭 데모는 M3/2단계 참고 자산. 단, 저장소를 push하면 Vercel 배포가 Next.js 앱으로 교체됨 — M1 배포(T-08) 전까지 push 보류.
- **영향**: T-02 (참조 원본), T-08 (배포 시 기존 정적 사이트 대체)

## D-04 (2026-07-19) 공유 링크 방식 → 무상태 URL 인코딩 (M1 한정)
- **선택**: 응답 18개를 18자리 숫자 코드로 URL에 인코딩(`/r/021122...`), 열람 시 채점·리포트 재생성. (기본값으로 결정)
- **이유**: M1에는 DB가 없음(Supabase는 T-09). 무상태 방식이면 배포 환경(Vercel serverless)에서도 저장소 없이 동작. 단점: 열람마다 리포트 재생성(LLM 연동 후 비용 발생) → M2에서 `diagnoses.share_token` + 저장된 리포트 방식으로 교체.
- **영향**: T-06 (구현), T-09 (교체 대상), T-14 (OG 이미지도 코드 기반으로 생성 가능), U-4 (공개 범위는 M2 게이팅 때 결정)

## D-05 (2026-07-19) LLM 제공자 → Google Gemini 1순위로 전환
- **선택**: Anthropic 결제 실패(해외결제 문제)로 리포트 생성 LLM을 `gemini-2.5-flash`(Google AI Studio, 무료 티어)로 전환. 폴백 체인: Gemini → Claude(`claude-sonnet-5`, 키·크레딧 확보 시 자동 사용) → mock.
- **이유**: Gemini AI Studio 키는 결제 수단 없이 무료 티어로 발급 가능. Claude 코드는 유지되어 있어 크레딧 확보 시 파일 수정 없이 복귀 가능(1-Pager 원안은 Claude Sonnet 5).
- **영향**: T-05 (완료 기준을 "실제 LLM 리포트"로 해석 — Gemini로 충족 가능), lib/llm.ts (교체 지점)

## D-06 (2026-07-19) Supabase 연결 시점 → T-08 직후로 앞당김
- **선택**: T-09(Supabase 연결)를 M2 결제 작업까지 기다리지 않고 T-08 배포 검증 직후 바로 진행.
- **이유**: 배포 후 실사용자 응답이 어디에도 저장되지 않아 M1 가설 검증 지표(완주 수·응답 분포)를 볼 수 없고, 공유 링크 열람마다 LLM 재호출 비용 발생. 데이터 전략(1-Pager)상 응답·리포트는 핵심 축적 대상.
- **영향**: T-09 (앞당김 — 저장 + 리포트 캐시 + share_token까지 포함), T-06 (무상태 링크 → DB 토큰 링크로 교체 시점도 함께)

## D-07 (2026-07-19) 무료/유료 경계 (U-2) → 공감 후킹까지 무료
- **선택**: 무료 = 헤드라인 + 레이더·축 레벨 + "한눈에 보기" + "이런 모습, 익숙하시죠?" / 잠금(990원) = "어쩌면 의외의 모습" + 축별 읽기 + 조언 + 학원 팁.
- **이유**: "우리 아이 얘기 같다"는 공감을 무료로 증명해 신뢰를 만들고, 가장 강력한 콘텐츠(의외성 인사이트·실천 조언)를 잠가 결제 동기를 극대화. 서술의 맛을 보여줘야 990원 전환이 일어난다.
- **영향**: T-10 (게이팅 구현 경계), T-11 (언락 CTA 위치 — 잠금 시작 지점), U-4 (공유 페이지도 같은 경계 적용 검토)

## D-08 (2026-07-20) 결제 자동 언락 → Groble 웹훅 + 구매 시 질문으로 진단 식별
- **선택**: Groble 웹훅(`payment.completed`)을 받아 자동 언락. 웹훅 페이로드에 커스텀 메타데이터·쿼리파라미터가 없어, 상품의 "구매 시 질문" 기능으로 "진단 결과 링크"를 필수 입력받아 `questionAnswers`에서 share_token을 추출하는 방식 채택.
- **이유**: Groble 지원팀 확인 결과 API 없음, 자동 전달 기능은 전 구매자 동일 링크만 지원(1인 1코드 불가). 웹훅+구매시질문 조합이 유일한 자동화 경로.
- **영향**: T-12 (자동 언락 엔드포인트), 관리자 언락 API(`/api/unlock`)는 웹훅 실패 시 폴백으로 유지

## D-09 (2026-07-20) 면책 문구 수준 (U-6) → 최소한만
- **선택**: 상세 면책 페이지(`/disclaimer`)나 다중 노출 지점 확대 없이, 현재의 짧은 문구("본 진단은 자녀 이해를 돕기 위한 참고 자료이며, 의학적·심리학적 진단을 대신하지 않습니다")를 기준으로 유지. 유일한 보강은 지금까지 빠져 있던 설문(`/survey`) 화면에도 동일 문구를 노출해 앱 전체(랜딩·설문·결과·리포트 본문)에 일관되게 배치하는 것.
- **이유**: 사용자가 "현재 수준으로 충분하다"고 판단. 결제 직전 별도 재노출이나 전용 약관 페이지는 이 단계에서 과함.
- **영향**: T-13 (완료 기준을 최소 보강으로 축소)

## D-10 (2026-07-21) 면책 문구 위치 재조정 + 리포트 톤 = 강점 반전
- **선택**: (1) 면책 문구를 랜딩 페이지 하단 1곳으로 축소(설문·결과·리포트 본문에서 제거). D-09의 "앱 전체 일관 배치"를 뒤집음. (2) 축 설명·리포트 톤을 "부모가 이미 아는 확인"이 아니라 "걱정하던 행동을 강점의 언어로 재해석"하는 반전 프레이밍으로 전환.
- **이유**: 사용자 피드백. 면책 반복이 과하고, 리포트의 차별점은 "우리 아이를 다르게 보게 만드는" 반전에 있음. 예: 유능감 낮음→신중함, 소진 높음→자기 조절, 수준부담 높음→도전 욕구, 자기주도 낮음→관계형 학습.
- **적용**: AXIS_META(questions.ts) positive/negative 양극단 모두 강점 언어, llm-mock AXIS_NARRATIVE, 프롬프트 v4(반전 지침 추가), 예시 리포트 v4 재생성. 걱정 케이스(유능감 0·소진 76·부담 92)에서 반전 검증 완료.
- **영향**: T-13(면책 위치), T-20(의외성과 함께 리포트 핵심 톤 확정)

## D-11 (2026-07-24) 문항 수 → 18개에서 25개로 재확장
- **선택**: 18문항이 "전문성이 없어 보일 수 있다"는 피드백에 따라 25문항으로 확장. 상대적으로 얇았던 축(수준 부담·소진 신호·사회성)에 각 2문항, 유능감에 1문항 추가 — 축당 4~5문항 교차 측정 구조 강화.
- **이유**: 완주 시간(5분 약속)을 크게 해치지 않는 선에서 정밀도·근거 있어 보이는 인상을 함께 잡는 절충. 문항을 무작정 늘리기보다 기존 문항과 겹치지 않는 다른 각도(교재 난이도 발화, 신체화 소진 신호, 새 환경 적응력, 과제 앞 자기암시 등)로 설계해 단순 물량이 아닌 다각도 측정임을 담보.
- **영향**: T-02(문항 정의 갱신), scripts/check-scoring.ts(검증 케이스 갱신, 21건 통과), lib/example-report.ts(25문항 기준 재생성 필요). 기존 무상태 18자리 공유 코드(legacy, DB 미저장분)는 길이 불일치로 더 이상 디코딩되지 않음 — DB 토큰 기반 공유가 주 경로라 영향 미미.

## D-12 (2026-07-26) 초기 배포 → 전체 무료 공개, 유료 전환은 나중에
- **선택**: `PAYWALL_ENABLED` 환경변수 스위치를 만들어, 미설정(기본값)이면 게이팅 없이 전체 리포트를 공개. `true`로 설정하면 코드 변경·재작업 없이 D-07 게이팅(무료 구간+"의외의 모습"부터 잠금)이 그대로 복원된다.
- **이유**: 사용자가 "우선 무료로 배포하고, 나중에 유료 전환 시 다시 말하겠다"고 결정. 초기에는 진입장벽을 없애 유입·검증에 집중하고, 결제 인프라(Groble·웹훅·게이팅 UI)는 이미 완성돼 있으니 나중에 스위치만 켜면 됨.
- **적용**: `lib/report-gate.ts`의 `splitReport()`가 `isPaywallEnabled()`를 먼저 확인 — false면 무조건 전체 마크다운 반환. `/api/report`, `/r/[code]`, 결과 화면 CTA 모두 이 함수 하나에 의존하므로 다른 코드 변경 없음. 로컬·브라우저에서 잠금 아이콘·990원 CTA·블러 전부 사라지고 전체 리포트가 나오는 것 확인.
- **주의(트레이드오프)**: 저장된 리포트 본문(`content_md`)은 항상 전체가 저장되고, 게이팅은 "읽을 때" 적용된다. 즉 나중에 `PAYWALL_ENABLED=true`로 전환하면, **무료 기간에 발급된 기존 공유 링크도 그 순간부터 다시 잠긴다** (선착순 사용자에게 "갑자기 유료화" 경험이 될 수 있음). 전환 시점에 이 트레이드오프를 다시 확인하고, 필요하면 전환 이전 진단은 계속 무료로 두는 예외 처리를 추가하기로 함.
- **영향**: T-10/T-11/T-12(게이팅·결제 흐름 — 코드는 유지, 스위치만 꺼짐), 향후 "유료 전환" 요청 시 Vercel에 `PAYWALL_ENABLED=true` 추가 + Redeploy만 하면 됨.

## D-13 (2026-07-26) 아이 연령대 반영 + "의외성" 문항 재설계
- **선택**: (1) 설문 시작 화면에 이름(선택)과 함께 아이 연령대(6단계: 미취학·초등 저/중/고·중학생·고등학생, 필수)를 입력받는다. (2) 프롬프트(v6)에 연령대별 발달 단계 해석 지침을 추가 — 미취학/저학년의 낮은 자기주도성·유능감을 "문제"가 아닌 자연스러운 발달로, 중고학년은 자기효능감·또래관계 전환기로, 중학생은 정체성·정서 기복을, 고등학생은 실제 입시 부담을 반영해 서술하도록 지시. (3) 25문항 중 5개(q7, q19, q20, q22, q23)를 부모가 "이런 생각은 해본 적 없다"고 느낄 만한 더 통찰력 있는 시나리오로 재작성 — 단, 옵션별 `effects` 값은 그대로 유지해 채점 로직·검증 스크립트는 변경 불필요.
- **이유**: 사용자가 문항에서 "의외성"(부모가 처음 생각해보는 관점)을 느끼길 원했고, 리포트도 나이에 안 맞는 기대치를 들이대지 않도록 발달 단계를 반영해야 한다고 요청.
- **적용**: `lib/age-bands.ts`(신규), `app/survey/page.tsx`(연령대 필수 선택 UI), `lib/prompt.ts`(v6, 연령대 해석 지침), `lib/questions.ts`(5문항 재작성), `lib/db.ts`/`supabase/schema.sql`/`migrations/005_child_age_band.sql`(child_age_band 컬럼), `app/api/report/route.ts`·`app/result/page.tsx`·`app/components/result-view.tsx`·`app/r/[code]/page.tsx`(childAgeBand 엔드투엔드 전달), `lib/llm-mock.ts`(폴백 리포트에도 연령대 문구 반영). `scripts/check-scoring.ts` 21건 그대로 통과 확인.
- **검증**: 로컬 브라우저로 연령대 "초등 중학년" 선택 → 25문항 응답 → Gemini가 실제로 "초등 중학년 아이들에게는 ~", "초등 중학년 시기는 자기효능감이 더욱 견고해지는 ~" 등 해당 발달 단계를 반영해 서술하는 것을 확인.
- **마이그레이션 순서(해결됨)**: 푸시 전 로컬 테스트에서 `migrations/005_child_age_band.sql` 미실행 상태를 먼저 잡았다(`PGRST204: Could not find the 'child_age_band' column` → 모든 `saveDiagnosis` 실패). 이전 `child_name` 사고와 같은 패턴이었지만 이번엔 배포 전에 발견. 사용자가 마이그레이션 실행 → 컬럼 존재·저장·조회 검증 후 커밋·푸시. **원칙: 새 DB 컬럼에 의존하는 코드는 마이그레이션 실행을 확인한 뒤에만 푸시한다.**
- **영향**: T-02(문항 갱신), T-04(프롬프트), T-09(스키마), 향후 학원 매칭(T-17) 시에도 연령대 데이터를 축적 지표로 활용 가능.

## D-14 (2026-07-26) 문항 톤 → "구체적 상황에서의 행동"을 묻는 시나리오로 전면 전환
- **선택**: D-13에서 5문항만 손봤던 것을 확대해, 25문항 중 추상적·자기범주화형 문항 13개(q2·q3·q4·q6·q8·q9·q10·q11·q12·q13·q14·q15·q18·q24)를 "특정 장면에서 아이가 실제로 어떻게 행동하는가"를 묻는 형태로 재작성. 문항 수는 25개 유지(D-11 결정 존중).
- **이유**: 사용자가 "이런 것도 물어보네?" 반응이 나올 문항을 더 늘리고, 구체적 상황에서의 행동을 묻는 문항을 늘리라고 요청. 부모가 "우리 아이는 사회성이 높은가?"처럼 스스로 범주화하게 하면 답이 부정확하고 진부하지만, "아는 아이가 거의 없는 생일파티에서 처음 30분 동안 아이는?"처럼 장면을 주면 관찰을 그대로 떠올리게 되어 정확도와 의외성이 동시에 올라간다.
- **예시**: 수업 수준 질문 → "문제를 풀다 말고 연필을 놓은 채 가만히 있는 모습, 얼마나 자주 보이나요?" / 숙제 소요시간 → "문제집 한 페이지를 풀 때 지우개를 쓰는 정도는?" / 칭찬 반응 → "유난히 좋은 점수를 받아온 날 가장 먼저 하는 말은?"(3번 선택지 "이번엔 문제가 쉬웠어" = 성공을 외부 귀인하는 신호) / 학습스타일 → "조립 설명서가 든 장난감을 함께 만든다면 먼저 무엇을 하나요?" / 깊이·넓이 → "아이 방을 둘러본다면 물건들은 어떤 모습인가요?"
- **불변 규칙 확립**: 문항 재작성 시 **옵션의 순서와 `effects` 값은 그대로 두고 text/label만 바꾼다.** 채점 축 의미·`axisRanges()`·`scripts/check-scoring.ts` 21건이 전부 그대로 유지된다. 이 규칙을 `lib/questions.ts` 헤더 주석에 명시.
- **부수 수정**: (1) q3 선택지에 있던 내부 주석("(자신감 부족)" 등)이 리포트에 그대로 인용돼 노출되던 문제 제거. (2) 프롬프트 v7 — 선택지를 `"..."`로 감싸던 것을 `「...」`로 바꿔 선택지 안 큰따옴표와의 중첩 해소하고, "선택지 문구를 통째로 따옴표로 옮기지 말고 자연스러운 서술로 풀어 쓸 것" 지침 추가. 재생성 결과 `""이번엔 문제가 쉬웠어"라고 해요"` 같은 어색한 인용이 사라지고 "좋은 점수를 받아온 날에도 '이번엔 문제가 쉬웠어'라며 자신의 공으로 돌리지 않는다고 하셨죠"처럼 자연스러워진 것 확인.
- **영향**: T-02(문항 갱신), T-04(프롬프트 v7). `lib/example-report.ts`는 이전 문항 기준으로 생성돼 있어 재생성 대상(우선순위 낮음).

## D-15 (2026-07-28) q5를 학원 전제 없는 장면 문항으로 교체 (D-14 누락분 보정)
- **선택**: q5 `학원 다녀온 날 아이 표정은요?` → `평일 아침, 아이를 깨울 때의 모습은 어떤가요?`로 교체. 옵션 순서·`effects`(burnout -2/-1/1/2)는 그대로.
- **이유**: 세 가지가 겹쳐 있었다. ① **D-14 누락** — D-14가 장면형으로 고친 13개 목록에 q5가 없어, 주변 문항이 전부 장면화된 뒤에도 q5만 "평소 표정 전반"을 묻는 옛 자기범주화 톤으로 남아 있었다. 사용자가 "진단 목적이 애매해 보인다"고 지적한 지점이 정확히 이것. ② **q14와 중복** — q14(`차 안·저녁 식탁에서 학원 이야기가 나왔을 때`)도 학원 소재 + burnout 단독 축 + 같은 방향이라, D-11이 "겹치지 않는 각도로 설계"한다던 원칙에서 이 둘만 벗어나 있었다. ③ **학원 등록 전제** — 설문은 이름·연령대만 받고 학원 여부를 묻지 않으며 모든 문항이 필수 선택이라(`app/survey/page.tsx`, "해당 없음" 없음), 학원에 다니지 않는 아이(미취학·초등 저학년 연령대가 존재)의 부모는 아무 답이나 고르게 되고 그 노이즈가 전부 burnout 축 한 곳으로 몰린다.
- **새 문항이 이 각도인 이유**: 아침 기상은 학원 등록 여부와 무관하게(미취학은 어린이집·유치원) 모든 부모가 매일 관찰하는 장면이고, 에너지 → 지침 → 회피의 4단계 기울기가 자연스럽게 얹힌다. 기존 장면과도 겹치지 않는다 — q24는 하교 후 대화, q11은 새 학기 교과서, q18은 시험 전날 밤. +2 선택지(`"오늘 안 가면 안 돼?"`)가 q21(신체화 회피)과 인접하지만, q21은 "아프다는 호소"를 q5는 "아침 에너지 수준"을 읽으므로 측정 기제가 다르다.
- **남은 학원 소재 문항**: q14·q21 둘은 유지. 문구상 "공부나 학원"처럼 대안을 함께 제시하거나 학원 외 상황으로도 답할 수 있어 q5만큼 전제가 강하지 않다. 다만 학원 전제를 완전히 없애려면 이 둘도 후보다.
- **검증**: `npx tsc --noEmit` 통과, `node scripts/check-scoring.ts` 전 항목 통과(burnout 이론 범위 -8~23으로 변동 없음 — effects를 건드리지 않았으므로 D-14 불변 규칙대로 채점 체계 전체가 그대로 유지됨).
- **영향**: T-02(문항 갱신). 프롬프트·스키마·채점 변경 없음. `lib/example-report.ts`는 D-14 시점부터 이미 재생성 대상(우선순위 낮음).

## D-16 (2026-07-28) 리포트 "축별로 읽어보기"에 축별 이론 인용 추가
- **선택**: 프롬프트 v8 — "축별로 읽어보기" 섹션에서 각 축을 관련 이론과 한 문장으로 연결하도록 지시 추가. 자기주도성·유능감·사회성은 자기결정성이론(SDT)의 자율성·유능감·관계성 욕구, 수준 부담은 근접발달영역(ZPD), 소진 신호는 학업 소진 연구. `lib/llm-mock.ts`에도 동일 매핑(`AXIS_THEORY`)을 추가해 폴백 리포트도 같은 수준을 유지.
- **이유**: 사용자가 "리포트에 이론 인용도 적절히 추가하여 신뢰도를 높여라"고 요청. D-11에서 문항 수를 늘린 이유도 "근거 있어 보이는 인상"이었고, 랜딩(`theory-section.tsx`)에는 이미 SDT·ZPD·학업소진 설명이 있지만 실제로 부모가 받아보는 리포트 본문에는 이론명이 전혀 등장하지 않았다 — 근거와 결과물이 분리돼 있던 지점.
- **경계**: T-14에서 헤더의 '교육심리학 기반' 배지를 "과한 강조"로 제거한 전례가 있다. 그 반성을 반영해 논문 인용투("~연구에 따르면")가 아니라 "이건 ~이론에서 말하는 ~예요" 식 대화체 한 문장으로, 축마다 정확히 한 번만 넣도록 못박았다. 학습 선호 방식(스타일·깊이/넓이)은 `theory-section.tsx`가 이미 "학계에서 근거가 충분치 않다"고 밝힌 지표라 이론을 붙이지 않는다. 의학적·심리학적 진단 표현 금지 규칙과는 무관 — 진단명이 아니라 이론적 틀 인용이라 저촉되지 않는다.
- **검증**: `npx tsc --noEmit`, `node scripts/check-scoring.ts` 통과. `generateMockReport()`를 임시 스크립트로 직접 호출해 5개 축 모두에 이론 문장이 자연스럽게 붙는 것 확인 후 스크립트 삭제(커밋 없음). 실제 Gemini/Claude 호출은 `/api/report`가 프로덕션 DB에 행을 남기므로 이번 검증에서는 실행하지 않음.
- **영향**: T-04(프롬프트 v8), `lib/llm-mock.ts`. `lib/example-report.ts`(랜딩 예시 리포트)는 v8 이전 프롬프트로 생성된 상태라 이론 문장이 없음 — D-14부터 이어진 재생성 대기열에 합류(우선순위 낮음, `scripts/make-example.js` 참고).

## D-17 (2026-07-28) 문항에 나이대별 장면 변형(`variants`) 도입 — 미취학 6문항 우선 적용
- **선택**: `Question`에 `variants?: Partial<Record<AgeBand, QuestionVariant>>` 필드 추가, `getQuestionsForAgeBand(ageBand)`가 나이대에 맞는 문항 배열을 반환. 변형은 **text/label만 교체하고 effects·순서·선택지 개수는 base와 완전히 동일** — D-14/D-15의 "옵션 순서·effects는 그대로" 불변 규칙을 나이대 축으로 확장한 것. 미취학(`preschool`) 변형을 6문항에 적용:
  - q4 `빨간 색연필 시험지` → `블록·퍼즐을 잘못 끼웠을 때`
  - q11 `새 학기 교과서` → `새 학기 새 책·활동 꾸러미`
  - q12 `좋은 점수 받아온 날` → `칭찬 스티커 받아온 날`
  - q15 `문제집 지우개 사용` → `선 따라 그리기·색칠 놀이`
  - q18 `시험 전날 밤` → `재롱잔치·발표 활동 전날 밤`
  - q24 `하교 후 오늘 학교 어땠어` → `하원 후 오늘 유치원(어린이집) 어땠어`
- **이유**: 사용자가 "미취학아동의 경우 교과서를 받았을 때의 반응이나 문제풀 때의 반응에 관한 문항은 적절치 않다"고 지적. 미취학 아동은 교과서·시험지·성적·정식 시험이라는 개념 자체가 없어(어린이집·유치원은 놀이 중심), 해당 문항에서 부모가 관찰할 근거가 없는 상태로 답하게 되고, 그 노이즈가 competence·zpd_strain·burnout 축에 섞인다 — D-15(q5)와 같은 성격의 문제.
- **각 변형의 측정 의도는 base와 동일**: q4·q11·q12는 "새로운/어려운 과제 앞에서의 반응"(유능감), q15는 "막힐 때 반복·포기 정도"(수준 부담), q18은 "평가·발표를 앞둔 긴장"(유능감/소진), q24는 "일과 공유 정도"(사회성) — 장면만 미취학의 실제 생활(어린이집·유치원, 블록·퍼즐, 색칠 놀이, 재롱잔치)로 옮겼다.
- **범위를 미취학으로 한정한 이유**: 초등 저학년(1~2학년)부터는 교과서·알림장·단원평가가 실제로 존재해 기존 문항이 대체로 유효하다고 판단. 다만 q4(시험지에 빨간펜 채점)와 q18(시험 전날 밤)은 초등 저학년에도 다소 이를 수 있음 — 정식 지필고사가 3학년 이후에 흔해지기 때문. 이번 라운드에서는 다루지 않고 향후 후보로 남긴다.
- **구현**: `lib/questions.ts`에 `getQuestionsForAgeBand()` 추가. `app/survey/page.tsx`(설문 렌더링), `lib/prompt.ts::buildUserPrompt`(LLM에 전달하는 실제 응답 장면), `lib/llm-mock.ts::pickScenes`(mock 리포트의 장면 인용) 세 곳에서 정적 `QUESTIONS` 대신 이 함수로 교체. `lib/scoring.ts`(axisRanges/scoreAnswers)·`lib/share.ts`(무상태 공유 코드)·`scripts/check-scoring.ts`는 옵션 순서·effects·id·개수만 사용하므로 base `QUESTIONS`를 그대로 써도 무방 — 변형은 표시 텍스트에만 영향.
- **검증**: `npx tsc --noEmit`, `node scripts/check-scoring.ts` 통과. 임시 스크립트로 `getQuestionsForAgeBand('preschool')`이 6문항 모두 effects/순서/개수 불변임을 확인(스크립트는 삭제, 커밋 없음). 브라우저에서 미취학 선택 시 q4가 블록·퍼즐 문항으로, 고등학생 선택 시 원래 시험지 문항 그대로 렌더되는 것을 실제 확인.
- **영향**: T-02(문항 구조 확장). `scripts/check-scoring.ts`·채점·공유코드 로직 변경 없음(base 구조 불변). `lib/example-report.ts`는 영향 없음(고정 텍스트, 특정 나이대 답변으로 이미 생성됨).

## D-18 (2026-07-28) 랜딩에 "어쩌면 의외의 모습" 부각
- **선택**: 세 곳을 수정. ① 히어로 서브카피에 "부모님도 몰랐던 **의외의 모습**까지"를 명시적으로 추가. ② 히어로 CTA 바로 아래에 amber 톤 카드(기존 결과 화면 언락 CTA와 같은 색 언어, `var(--amber-light)`/`var(--amber-border)`)를 신설해 "겉모습 → 실제 의미" 반전 예시 2개를 미리 보여줌. ③ 하단 "우리 아이를 다시 보게 돼요" 카드 설명에 리포트의 실제 섹션명("어쩌면 의외의 모습")을 명시해 위 카드와 연결.
- **반전 예시 문구의 출처**: 새로 지어내지 않고 `lib/prompt.ts`의 시스템 프롬프트에 이미 박혀 있는 공식 예시("집중 못 함"→"자극이 부족할 때 다른 걸 찾는 호기심", "말을 안 들음"→"자율성 욕구가 강한 주체적인 아이")를 그대로 재사용. `lib/example-report.ts`(예시 리포트)의 실제 "어쩌면 의외의 모습" 문구는 D-15 이전 q5("학원 다녀와서 피곤해하고...")를 인용하고 있어 이번엔 쓰지 않음 — 이미 사라진 문항 전제를 랜딩에 다시 노출하는 꼴이 되기 때문.
- **이유**: 사용자가 "자녀의 어쩌면 의외의 모습을 알려준다는 점을 랜딩페이지에서 부각하라"고 요청. 기존 랜딩은 이 반전 프레이밍(D-10)이 리포트의 "백미"(`prompt.ts` 표현)라는 점을 전혀 드러내지 않고 있었다 — 문구가 없어 방문자가 이 제품의 핵심 차별점을 모른 채 이탈할 수 있었다.
- **배치 판단**: 히어로 바로 아래(첫 스크롤 안쪽)에 둬 가장 눈에 띄게 했다. 다만 T-14의 '교육심리학 기반' 배지 제거 전례(과한 강조 지양)를 의식해, 클레임만 반복하지 않고 **구체적 예시 2개**로 보여주는 방식을 택했다 — 추상적 배지보다 설득력 있고, 과하다는 인상도 덜하다.
- **검증**: `npx tsc --noEmit` 통과. 브라우저에서 카드가 히어로와 "우리 아이 학습 유형을 알면" 사이에 정상 렌더되는 것, 콘솔 에러 없는 것 확인.
- **영향**: `app/page.tsx`만 변경. 리포트 생성 로직·프롬프트·채점 변경 없음.

## D-19 (2026-07-28) 문항을 부모가 공감하는 대표 갈등 장면으로 강화
- **선택**: 문항의 장면·선택지 문구를 부모들이 실제로 겪는 대표적 갈등 순간("폭발 포인트")으로 끌어올림. base 5건 + 나이대 변형 4건:
  - base: q1 `모르는 문제가 나오면` → `숙제든 만들기든, 하던 것이 잘 안 풀려 막혔을 때`(문제 풀이 전제도 제거 — D-15 원칙), q9 옵션4에 `"이따 할게"만 반복` 문구, q17 `좋아하는 놀이나 취미가 생기면` → `무언가에 새로 꽂혔을 때(공룡·게임·아이돌·만들기 등) 그 뒤엔`, q22에 `친구와 다투거나 야단맞은 날처럼` 예시 추가
  - 변형: q4 `elem_low`(받아쓰기 공책 빨간펜 — 저학년 부모의 대표 장면), q18 `elem_low`(받아쓰기·발표 전날 밤), q13 `middle`/`high`(생일파티·캠프 → 새 학기 친한 친구 없는 반 — 십대에게 생일파티는 어색한 장면)
- **이유**: 사용자가 랜딩의 반전 예시("집중을 잘 못해요→호기심")처럼 "해당 나이대에서 나타나는, 부모를 폭발하게 하는 대표 상황·사례를 문항에 반영하면 부모가 공감하며 문항에 몰두할 것"이라고 요청. D-14가 장면형 전환을 했다면 D-19는 그 장면을 "아무 장면"이 아니라 "부모가 즉시 알아보는 장면"으로 조준하는 것.
- **경계 (이론 유지)**: ① D-14 불변 규칙 준수 — effects·순서·개수 불변, text/label만 수정. 전 나이대 무결성 스크립트로 확인. ② **문항 문구는 중립 관찰형 유지** — "폭발 상황"을 문항 문구에 감정적으로 싣으면("속 터지게 미룰 때") 부정 응답을 유도해 채점이 왜곡된다. 공감 포인트는 장면 선택과 선택지 속 아이의 말("이따 할게")에 싣고, 문항 자체는 중립을 지켰다. ③ 각 수정의 측정 의도 불변: q1(막힘 앞 반응→autonomy/zpd), q9(자발적 시작→autonomy), q13(낯선 또래 집단 진입→social), q17(관심사 지속 패턴→focus), q22(정서 회복 탄력→burnout).
- **검증**: `npx tsc --noEmit`, `check-scoring.ts` 21건, 전 나이대(6개) × 25문항 effects/순서/개수 불변 스크립트 통과(임시, 삭제). 브라우저에서 초등 저학년 선택 시 q4가 받아쓰기 문항으로 렌더되는 것 확인.
- **2차 적용 (같은 날 이어서)**: 사용자 승인 후 남겨뒀던 후보 처리. ① q6 `middle`/`high` — `조립 장난감` → `새 전자기기나 조립 가구`(십대에게 장난감은 어색). 영상 튜토리얼은 시각+청각이 섞여 스타일 축 매핑이 흔들리므로 선택지에 넣지 않고 4개 채널(그림/묻기/만지기/글)을 유지. ② q10 `middle`/`high` — `TV나 책` → `유튜브나 일상 대화`. ③ q23 `middle`/`high` — `인형이나 혼잣말` → `혼잣말이나 메모`(십대에게 인형은 어색). ④ q2 `preschool` — `문제를 풀다 말고 연필을 놓은 채` → `종이접기·한글 따라 쓰기처럼 앉아서 하는 활동`(사용자가 처음 지적한 "문제 풀 때의 반응" 문항인데 D-17에서 누락됐던 건).
- **변형 커버리지 현황**: q2(preschool), q4(preschool·elem_low), q6(middle·high), q10(middle·high), q11·q12·q15·q24(preschool), q13(middle·high), q18(preschool·elem_low), q23(middle·high).
- **영향**: T-02(문항 갱신). 채점·공유코드·프롬프트 버전 변경 없음. `lib/example-report.ts` 재생성 대기열 유지(우선순위 낮음).

## D-20 (2026-07-28) 문항 장면은 부모가 직접 관찰 가능해야 한다
- **원칙**: 문항의 장면은 **부모가 그 자리에서 직접 볼 수 있는 것**이어야 한다. 이 제품의 전제가 "부모의 관찰"인데, 부모가 볼 수 없는 장면(교실 안, 부모 없는 캠프 등)을 물으면 부모는 추측으로 답하게 되고 그 답은 관찰이 아니라 부모의 인상·기대를 측정하게 된다. D-15(상황 전제 금지)·D-19(공감 장면)에 이은 세 번째 문항 검증 기준.
- **계기**: D-19에서 만든 q13 십대 변형(`새 학기 첫날, 친한 친구가 한 명도 없는 반이 됐을 때`)에 대해 사용자가 "부모가 학교에 가서 보지 못하는 경우가 허다하다"고 지적. 교실은 부모의 관찰 범위 밖이라는 것.
- **수정**: ① q13 `middle`/`high` 변형 → `명절이나 가족 모임에서 처음 보거나 오랜만에 만나는 또래(사촌 등)와 함께 있게 됐을 때` — 부모가 그 자리에 있고, 낯선 또래 진입 행동(사회성 축)이 그대로 드러나며, "끝까지 겉돌거나 혼자 휴대폰만 봐요"라는 십대 특유의 공감 장면(D-19)도 살린다. ② base q13의 `캠프`도 같은 원칙 위반이라 제거 → `처음 간 놀이터나 아는 아이가 거의 없는 생일파티` — 3번 선택지("부모 옆에 붙어 있어요")가 원래부터 부모 동반을 전제하고 있었으므로 캠프와는 모순이었다.
- **점검**: 나머지 문항은 전부 가정 내 장면(아침 기상, 식탁 대화, 숙제, 하교 후 대화 등)이라 위반 없음. q24(하교 후 대화)·q14(식탁에서 학원 얘기)는 학교·학원에서 있었던 일이 소재지만 묻는 것은 "집에서 부모에게 보이는 반응"이므로 원칙에 부합.
- **검증**: `npx tsc --noEmit`, `check-scoring.ts` 통과. `getQuestionsForAgeBand`로 middle/high는 가족 모임 장면 + effects 불변, 다른 나이대는 base 그대로임을 확인.
- **영향**: T-02(문항 갱신). 채점·공유코드 변경 없음.

## D-21 (2026-07-28) 25→30문항, 6축×5문항 구조 재편 + 학원 유무 2중 분기
사용자가 "자녀에 대한 평가라 진중한 느낌이 필요하다"며 4가지를 요청: ① 문항을 6축×5문항(자율성·동기/학습 수준·격차/정서·번아웃/유능감/학습스타일·강점/관계·사회성)으로 재편 ② 학원 미경험 아동을 위한 인트로 분기 ③ 진단 깊이가 체감되도록 축 라벨·인터루드·근거 문구 추가 ④ 가격 표시·베타 표시 변경. 착수 전 사전 질문으로 두 가지 불일치를 확인했다.

- **유능감 축의 행방**: 사용자가 요청한 5축 목록에는 유능감이 없었지만, 실제로는 이미 5번째 채점 축(competence)으로 존재하고 D-16 이론 인용·레이더 차트(이미 오각형)에 쓰이고 있었다. **선택(사용자 확답)**: 유능감을 없애거나 다른 축에 합치지 않고 **6축×5문항(30문항)**으로 유지 — "축마다 문항 수를 맞춘다"는 원래 취지와 총 30문항이라는 목표를 그대로 지키면서 기존 채점 자산도 보존한다.
- **학습스타일·강점의 성격**: style(시각/청각/체험/읽기)·focus(깊이/넓이)는 원래 최빈값 기반 범주형 데이터라 다른 4축처럼 0~100 점수화가 안 된다. `QuestionCategory`(= `AxisId | 'style_strength'`)라는 채점과 무관한 UI 전용 필드를 신설해, 점수 축 5개(자율성·동기/학습 수준·격차/정서·번아웃/유능감/관계·사회성)와 범주형 묶음 1개(학습스타일·강점)를 합쳐 "6개 영역"으로 표시한다. 레이더 차트(`radar-chart.tsx`)는 원래부터 점수 축 5개만 그리므로 변경 없음 — "5각형으로 업데이트"해 달라는 요청은 이미 충족돼 있었다.
- **문항 재배분**: 기존 25문항을 축별로 분류(주 효과 기준)한 결과 자율성·동기 4, 학습 수준·격차 4, 정서·번아웃 5, 유능감 4, 사회성 4, 스타일+포커스 4 — 정서·번아웃만 5였고 나머지는 1개씩 부족했다. 신규 5문항(q26~q30)을 D-15/D-19/D-20 원칙(상황 전제 금지·소재 비중복·부모 관찰 가능)에 맞춰 작성해 각 5개로 채웠다: q26(숙제 순서 결정권, 자율성) · q27(정해진 분량 완료 소요시간, 학습 수준·격차) · q28(처음 해보는 것 앞에서의 반응, 유능감 — q25와 달리 "완전히 새로운 도전"이라는 별도 각도) · q29(형제·친구와 협동 활동 시 역할, 관계·사회성) · q30(재미있었던 일을 이야기하는 방식, 학습스타일). "경쟁 상황 반응"·"교사와의 관계"는 사용자 스펙에 있었지만, 관계·사회성 축의 기존 극성(긍정=함께일 때 힘 / 부정=혼자가 편함)에 억지로 끼워 맞추면 근거 없는 채점이 되므로 이번엔 넣지 않았다(추후 별도 논의 필요).
- **학원 유무 2중 분기**: 사용자가 "전 문항에 2중 분기 도입"을 선택해, `Question.hagwonVariants?: Partial<Record<HagwonStatus, QuestionVariant>>`를 나이대 `variants`와 별개 필드로 추가하고 `getQuestions(ageBand, hagwonStatus)`가 둘 다 적용한다(hagwonVariants가 나중에 적용돼 우선). 실제로 "학원"을 직접 언급하는 문항은 q14·q21 두 개뿐이었다 — 나머지는 D-15~D-20에서 이미 학원 전제를 제거했거나(예: 옛 q5), "공부나 학원"처럼 대안을 함께 제시해 원래도 무경험 가정에 답할 수 있었다. 사용자 예시("학원 다녀온 날 표정은?")는 D-15에서 이미 없앤 옛 q5를 가리키고 있어, 실제 남은 작업 범위는 예시보다 좁았다 — 이 점을 사용자에게 설명하고 진행.
- **설문 인트로**: `lib/hagwon-status.ts` 신설(age-bands.ts와 동일 패턴). 인트로 화면에 나이대 선택 옆에 "현재 학원이나 과외를 다니고 있나요?" 2지 선택을 추가, `sessionStorage`에만 저장(DB 미저장 — 아래 참고). "30문항 · 약 10분 · 6개 영역 측정" 문구 명시.
- **DB에는 저장하지 않음**: `child_age_band`(D-13)처럼 컬럼을 추가하면 CLAUDE.md가 경고하는 마이그레이션 순서 사고(`PGRST204`)를 사용자 확인 없이 재현할 위험이 있다. `hagwonStatus`는 클라이언트→`/api/report` 요청 본문→프롬프트에만 흐르고, 생성된 리포트 텍스트는 기존 `content_md` 컬럼에 그대로 저장되므로 공유 링크 재열람 시에도 톤이 보존된다. 다만 "학원 유무별 진단 수" 같은 구조화된 집계는 지금 불가능 — 필요해지면 T-16(데이터 축적 지표)과 함께 마이그레이션을 추가한다.
- **진단 깊이 체감 UI**: `getQuestions()`가 반환하는 배열의 `category` 필드로 문항 상단에 "{축 라벨} n/5"를 표시(`CATEGORY_META`). 10번째·20번째 문항 응답 직후 인터루드 화면을 끼워 넣어 `scoreAnswers()`로 그 시점까지의 부분 응답을 채점하고, 50점 기준 편차가 가장 큰 축을 골라 "지금까지의 응답에서 {축} 성향이 보이기 시작했어요" + 해당 축의 반전 프레이밍 문장(`AXIS_META`)을 보여준다 — 실제 응답을 반영하되 확정적 진단처럼 들리지 않게 기존 문구를 재사용했다. 리포트 쪽은 프롬프트에 "축별로 읽어보기의 각 축 문단 끝에 정확히 '(문항 5개 응답 종합)'을 덧붙인다"는 지시를 추가하고, `llm-mock.ts`에도 동일하게 하드코딩해 폴백 리포트도 같은 근거 문구를 갖게 했다.
- **"학원을 고른다면" 섹션 분기**: 헤딩 텍스트는 그대로 두고(D-14가 "## 헤딩 문구가 report-gate.ts와 정확히 일치해야 한다"고 못박은 전례 때문에 조건부 헤딩은 위험 부담이 큼) 프롬프트 지시로 톤만 분기 — 이미 다니면 "지금 다니는 곳이 맞는지 점검", 아직이면 "첫 학원 고르는 기준". `llm-mock.ts`의 `academyTips`도 동일 분기 문구를 추가하고, 관계·사회성 축 기반 추천에 "협동 활동 중심" 문구를 보강해 사용자가 요청한 "협력형 학원 구분"을 부분 반영했다(경쟁형 구분은 위 이유로 보류).
- **AXIS_META 라벨 변경**: autonomy "자기주도성"→"자율성·동기", zpd_strain "수준 부담"→"학습 수준·격차", burnout "소진 신호"→"정서·번아웃", social "사회성"→"관계·사회성" (competence "유능감"은 유지). 이 객체를 참조하는 프롬프트·mock·레이더차트·리포트가 전부 자동으로 새 라벨을 쓴다.
- **알려진 미해결 부작용**: `lib/example-report.ts`(랜딩 "예시 리포트 보기")는 v8 이전 프롬프트로 고정 생성된 텍스트라 옛 라벨("자기주도성" 등)과 옛 문항 기준 서술을 그대로 담고 있다. 레이더 차트는 `scoreAnswers()`로 **실시간** 재계산되어 새 라벨·새 범위(30문항 기준)를 쓰므로, 이번 변경으로 차트 라벨과 본문 서술의 불일치가 이전보다 더 눈에 띄게 됐다(예: 차트는 "자율성·동기", 본문은 "자기주도성"). D-14 때부터 재생성 대기열에 있던 항목인데 이번에 우선순위가 올라감 — 재생성하려면 실제 LLM 호출이 필요해 이번 세션에서는 하지 않음.
- **legacy 공유 코드 자릿수 변경**: `share.ts`의 무상태 코드는 문항 수만큼 자릿수를 쓰므로 25→30자리로 바뀐다. D-11 때와 동일한 패턴 — DB `share_token`이 주 경로라 영향 미미.
- **검증**: `npx tsc --noEmit`, `check-scoring.ts`(문항 수 정확히 30, 카테고리별 정확히 5문항 검사 추가) 통과. 나이대(6) × 학원유무(2) + null 조합 21가지에서 `getQuestions()`가 effects/순서/개수/category를 안 건드리는지 임시 스크립트로 확인 후 삭제. 브라우저로 초등 저학년+학원 미경험 경로를 답변하며 문항 라벨("자율성·동기 1/5" 등), 10번째 문항 후 인터루드, q14의 학원-미경험 변형 렌더를 확인 — `/api/report` 제출(30번째 문항)까지는 진행하지 않음(프로덕션 DB 쓰기 방지).
- **영향**: T-02(문항 전면 개편), lib/questions.ts·lib/hagwon-status.ts(신설)·app/survey/page.tsx·lib/prompt.ts(v9)·lib/llm-mock.ts·app/api/report/route.ts·app/result/page.tsx·app/components/result-view.tsx·scripts/check-scoring.ts. `lib/scoring.ts`·`lib/share.ts`는 구조만 그대로 재사용해 변경 없음.

## D-22 (2026-07-28) 가격 표시 "런칭 기념" 프레이밍 + 베타2 배지
- **선택**: 990원 CTA를 "런칭 기념 990원으로 전체 리포트 열기"로, 그 아래 "정가 8,000원 예정 · 지금은 런칭 기념가로 열람할 수 있어요" 문구를 추가(`app/components/result-view.tsx`, `PAYWALL_ENABLED=true`일 때만 노출). 레이아웃 헤더에 "베타2" 배지를 로고 옆에 상시 표시(`app/layout.tsx`, 전 페이지 공통).
- **이유**: 사용자가 처음엔 990원→8,000원으로 문구를 바꿔달라고 했으나, 실제 Groble 결제 페이지 가격은 대시보드에서 별도로 바꿔야 한다는 점을 알리자 "런칭 기념 기간엔 990원으로 진행하고 추후 8,000원 예정이라 밝히자"로 수정 요청 — 사이트 문구와 실제 결제 가격의 불일치를 원천 차단하는 방향.
- **정액권 정리**: "정액권 언급 삭제"를 요청했으나 확인해보니 `NEXT_PUBLIC_GROBLE_PACK10_URL`(10회권 8,000원)은 애초에 어떤 화면에도 렌더링되는 CTA가 없어 이미 사용자 눈에 안 보이는 상태였다. `single_990`/`pack10_8000`는 `app/api/unlock/route.ts`·`app/api/webhooks/groble/route.ts`의 결제 검증 로직에 남아 있는데, 이건 실제 결제 인프라라 문구 정리와 별개로 건드리지 않았다(과거 구매 건 처리·재활성화 가능성 보존).
- **FAQ**: 요청받은 "FAQ 문구 수정"은 대상을 찾지 못했다 — 사이트에 FAQ 섹션 자체가 없다. 새로 만들지는 않고 보고만 함.
- **검증**: `npx tsc --noEmit` 통과. 브라우저에서 헤더 베타2 배지 렌더 확인. 가격 CTA는 `PAYWALL_ENABLED` 기본 미설정(D-12, 전체 무료 공개) 상태라 지금 화면엔 노출되지 않음 — 코드 확인으로 검증.
- **영향**: `app/components/result-view.tsx`, `app/layout.tsx`.

## D-23 (2026-07-28) 인터루드 문구 모순 수정 + 리포트 분량·이론 인용 강제 (프롬프트 v10)
- **인터루드 버그**: D-21에서 만든 10·20번째 문항 후 인터루드가 "지금까지의 응답에서 {축 라벨} 성향이 보이기 시작했어요" + `AXIS_META`의 positive/negative 문장을 이어 붙였는데, 축이 **negative**(낮은) 방향일 때 라벨 그대로("정서·번아웃 성향이 보이기 시작했어요")와 실제 문장("배움에 에너지가 살아 있는, 지금이 몰입하기 좋은 때예요" — 즉 번아웃이 낮다는 뜻)이 정면으로 모순돼 보였다. 사용자가 정확히 이 조합을 지적. **수정**: 헤드라인을 축 이름을 언급하지 않는 중립 문구("지금까지의 응답에서 이런 모습이 보이기 시작했어요")로 바꾸고, 축 라벨은 상단의 진행률 텍스트에 "10/30 완료 · 정서·번아웃"처럼 태그로만 붙여 방향성을 주장하지 않게 했다. 의미는 `AXIS_META`의 문장이 전부 담당 — 이 문장들은 이미 양극단 모두 강점 언어(D-10)라 그 자체로는 모순이 없다.
- **리포트 분량 저하**: 사용자가 "리포트가 지난번보다 단순해졌다"며 지난번 수준의 구체적·상세한 서술과 이론 인용을 요청. D-16(이론 인용)·D-21("문항 5개 응답 종합", 학원 톤 분기) 등 지침이 누적되면서, 모델이 형식 요건을 채우는 데 주의를 쓰느라 정작 서술 자체가 짧아졌을 가능성이 크다고 판단(정확한 원인은 실제 생성 로그를 못 봐서 단정할 수 없음 — Gemini/Claude 호출이 실패해 mock으로 폴백했을 가능성도 배제 못 함, 다만 `.env.local`에 두 키 모두 설정돼 있는 건 확인함).
- **선택**: 프롬프트 v10 — ① 지침 목록 맨 앞에 "분량과 구체성이 최우선" 방점을 추가해, 아래 형식 지침이 많아도 서술을 압축하지 말라고 명시. ② 섹션별 최소 분량을 구체적 숫자로 못박음 — 한눈에 보기 3~4문장→**4~5문장**, 이런 모습 익숙하시죠 2~3개→**3~4개**, 의외의 모습 각 항목 문장 수 명시(**3문장 이상 + 근거**), 축별로 읽어보기 2~3문장→**3~4문장 + 실제 응답 1개 이상 인용**, 이렇게 도와주세요 각 항목 **2문장 이상**(무엇을+왜), 학원을 고른다면 **4문장 이상**. ③ 이론 인용 지침을 "다섯 축 중 하나라도 빠지면 안 된다"로 못박아 선택적으로 읽히지 않게 했다. ④ `llm-mock.ts`의 장면 인용 개수도 3→4로 맞춤(폴백 리포트도 같은 최소 기준을 흉내 내도록).
- **검증**: `npx tsc --noEmit`, `check-scoring.ts` 통과. mock 리포트를 임시 스크립트로 생성해 장면 4개·5개 축 전부 이론 문장 포함·"(문항 5개 응답 종합)" 포함 확인 후 스크립트 삭제(커밋 없음). 브라우저에서 인터루드가 더 이상 축 이름을 직접 주장하지 않는 것 확인. 실제 Gemini/Claude 출력이 이번 지침으로 실제로 길어지는지는 `/api/report`를 호출해야 확인 가능한데 프로덕션 DB 쓰기 방지 원칙상 이번엔 호출하지 않음 — **다음 실제 진단에서 분량이 다시 짧으면 알려달라.**
- **영향**: `app/survey/page.tsx`(인터루드), `lib/prompt.ts`(v10), `lib/llm-mock.ts`.

## D-24 (2026-07-28) 재해석 인사이트 매핑 테이블 + 레벨 숫자 의미 설명 (프롬프트 v11, 베타3)
사용자의 2차 피드백 분석 결과 두 가지: ① 지불 의사를 결정하는 건 "몰랐던 걸 알게 되는 순간"이라, 응답 조합이 평범해도 "어쩌면 의외의 모습" 재해석 문장이 최소 1개는 반드시 나와야 한다. ② 결과 화면의 "레벨 X / 5"가 숫자만 덩그러니 보여서, 그 숫자가 구체적으로 뭘 뜻하는지 알 수 없었다.

- **재해석 인사이트**: `lib/insights.ts` 신설. "부모님은 [흔한 오해]로 보셨을 수 있지만, 사실 ○○는 [재해석]이에요" 구조의 문장을 2축 조합 규칙 12개 + 단일 축 폴백 10개(5축×상/하)로 매핑. 콤보 규칙이 매칭되면 우선 쓰고, 부족하면 편차(|정규화점수-50|)가 가장 큰 축부터 단일 폴백으로 채운다 — **243가지 축 조합(5축×3구간) 전수 스크립트 검사에서 항상 최소 2개 반환 확인**(0개인 경우 없음). D-16(AXIS_THEORY)과 같은 패턴 — mock은 이 문장을 그대로 쓰고, 실제 LLM에는 `[재해석 후보]`로 프롬프트에 주입해 "최소 1개는 반드시 녹여 쓴다"고 지시한다(사용자 예시 "자율성 욕구가 관계성 욕구와 강하게 연결된 아이예요"를 규칙 중 하나로 그대로 반영). 이름 뒤 은/는 조사는 한글 종성 유무로 자동 판별하는 `withTopic()` 헬퍼로 처리.
  - **왜 프롬프트 지침만으로 안 끝냈나**: D-23에서 지침을 강화했는데도 "리포트가 단순해졌다"는 피드백이 있었던 전례 — LLM의 지침 준수는 확률적이라, "반드시 1개"라는 요구는 코드가 후보 문장을 계산해 재료로 쥐여주는 쪽이 훨씬 안전하다고 판단했다.
  - **기존 `surprises` 배열 대체**: `llm-mock.ts`에 있던 4개 규칙짜리 즉석 배열을 이 공용 매핑으로 교체 — 로직 중복 제거, 커버리지 대폭 확대(4→12 콤보 + 10 폴백).
- **레벨 숫자 의미**: `lib/questions.ts`에 `LEVEL_MEANING: Record<AxisId, [string,string,string,string,string]>` 추가 — 5축 × 레벨 1~5, 총 25개 문장. `app/components/result-view.tsx`의 "레벨 X / 5" 배지 바로 아래에 amber 색상으로 표시하고, 기존 반전 프레이밍 문장(positive/negative)은 그 아래 그대로 유지 — 전자는 "숫자가 뭘 뜻하는지"(구체적·실용적), 후자는 "어떻게 긍정적으로 볼지"(강점 언어)로 역할이 다르다.
  - **톤 조정**: 사용자가 준 예시(정서·번아웃 레벨 4 "학습량 조정을 권장해요", 레벨 5 "즉시 정서적 돌봄이 우선이에요")는 임상적·경보성 표현에 가까워 D-09("의학적·심리학적 진단 표현 금지")·D-10(반전 프레이밍)과 충돌한다고 판단, 의도(그 레벨에서 실제로 무엇이 다른지 구체적으로 알려주기)는 살리되 표현은 부드럽게 조정했다 — 예: 레벨 5를 "즉시 돌봄이 우선"이 아니라 "지금은 학습보다 회복과 대화가 먼저예요"로. 다른 4개 축도 같은 원칙으로 작성.
  - **적용 범위**: "숫자만 보이는 곳" 전수 조사 결과 실제 해당하는 곳은 `result-view.tsx`의 이 지점 하나뿐이었다(레이더 차트는 0~100 정규화 점수를 보여주는 것이라 "레벨"이 아니고, mock 리포트의 "(레벨 X/5)"는 이미 바로 뒤에 서술이 붙어 있어 바깥 지침의 "바로 옆/아래" 요구를 이미 충족).
- **베타3**: `app/layout.tsx` 헤더 배지 베타2→베타3.
- **검증**: `npx tsc --noEmit`, `check-scoring.ts` 통과. 5축×3구간(low/mid/high) 전수 243조합에서 재해석 인사이트 0개인 경우 없음(최소 2개) 확인, `LEVEL_MEANING` 5×5 전부 채워진 것 확인 — 둘 다 임시 스크립트로 검증 후 삭제(커밋 없음). `/report/example`(고정 리포트라 `/api/report` 호출 없이 점수만 실시간 계산됨)로 브라우저에서 5축 모두 "레벨 X/5" 아래 새 의미 문장이 정상 렌더되는 것, 콘솔 에러 없는 것, 헤더 베타3 확인. mock 리포트를 임시 스크립트로 생성해 "어쩌면 의외의 모습"에 새 문형("부모님은 ~로 보셨을 수 있지만, 사실 지호는 ~") 2개가 나오는 것 확인.
- **영향**: `lib/insights.ts`(신설), `lib/questions.ts`(LEVEL_MEANING), `lib/prompt.ts`(v11), `lib/llm-mock.ts`, `app/components/result-view.tsx`, `app/layout.tsx`.

## D-25 (2026-07-28) 적응형 보조 질문 + 로딩 화면 재설계 + 공유 흐름 강화
사용자의 3차 피드백 세 가지: ① 응답 신뢰도가 낮으면(“그런 적 없다/모르겠다” 다발) 보조 질문으로 보강 ② 로딩 화면이 광고처럼 보임 ③ 지인 추천(“사주보기”형) 기반 성장을 위한 공유 강화.

- **"잘 모르겠어요" 옵션**: 착수 전 확인해 보니 기존 문항엔 그런 응답 유형이 아예 없었다(요청의 전제가 현재 코드와 달랐던 케이스). `QuestionOption`에 `uncertain?: boolean` 필드를 추가하고, 채점 축 25문항(스타일·포커스 5문항 제외) 전부에 다섯 번째 선택지로 "아직 못 봤거나 잘 모르겠어요"(`UNCERTAIN_OPTION`, effects 없음)를 붙였다. 기존 나이대·학원유무 변형(`variants`/`hagwonVariants`)은 라벨 배열 길이가 4개뿐이라도 `v.options[i] ?? opt.label` 폴백 덕분에 그대로 동작 — 5번째 옵션은 항상 기본(비-변형) 문구를 쓴다.
- **보조 문항**: `SUPPLEMENTARY_QUESTIONS`(축당 1개, 총 5개, id는 `sup_`로 시작해 base와 절대 안 겹침)를 신설. "옷 고르기·퍼즐 단계·저녁 기운·새 규칙 배우기·밥 먹을 때 선호"처럼 놀이·식사 등 더 일상적인 장면으로 재설계했다(사용자 예시 그대로). base 30문항엔 포함되지 않고 `app/survey/page.tsx`가 조건부로만 보여준다.
- **트리거 로직**: `AXIS_BLOCK_ENDS`로 5문항 축 블록이 끝나는 시점(step 5/10/15/20/30 — style_strength인 25는 채점 축이 아니라 제외)을 감지, 그 블록에서 "잘 모르겠어요"가 2회 이상이면 해당 축의 보조 문항을 "{축} 성향을 더 정확히 보기 위한 추가 질문이에요" 배너와 함께 끼워 넣는다. 진행률 표시는 유지하고("6/30" 등) 보조 문항 화면만 별도 카드로 분리. 축당 한 번만 트리거되도록 `triggeredAxes`로 방어.
- **채점 반영**: `scoring.ts`가 `ALL_SCORABLE_QUESTIONS = QUESTIONS + SUPPLEMENTARY_QUESTIONS`를 기준으로 `axisRanges()`·`scoreAnswers()`를 계산하도록 확장. `AxisScore`에 `answeredCount`(그 축에서 "잘 모르겠어요"를 뺀 실제 신호 응답 수, 보조문항 포함)를 추가 — 보조 문항을 안 본 사용자는 range가 아주 살짝(문항 하나 몫) 넓어질 뿐 실질 영향은 없다("부분 응답" 처리와 같은 원리).
- **"(문항 N개 응답 종합)" 동적화**: 지난 세션(D-21/D-23)에서 하드코딩했던 "5개"를 실제값으로 교체 — `describeScores()`가 축마다 "문항 N개 응답 기반"을 표시하고, 프롬프트(v12)는 "임의로 5로 고정하지 않고 [채점 결과]에 표시된 숫자를 그대로 옮겨 쓴다"고 지시, `llm-mock.ts`도 `scores.axes[axis].answeredCount`를 직접 참조. "잘 모르겠어요"로 답한 문항은 "이런 모습, 익숙하시죠?"·"축별로 읽어보기"에서 인용하지 않도록 프롬프트에 명시(인용할 실제 행동이 아니므로).
- **로딩 화면**: `report-loading.tsx`를 재설계 — "{이름}의 응답 {N}개를 분석하고 있어요. 잠시만 기다리시면 상세 해설 리포트가 나와요" + "응답 분석 → 성향 분류 → 리포트 작성" 3단계 체크 애니메이션(경과 시간 기준 순차 활성화). 기존의 "가장 두드러지는 축은 ○○예요" 같은 후킹성 힌트 문구는 뺐다 — 그게 "광고처럼 보인다"는 지적의 원인으로 보였다. 완료 시 자동 전환은 이미 `result-view.tsx`의 `useEffect`가 하고 있어(클릭 불필요) 추가 작업 없음.
- **공유 흐름**: 기본 공유 문구(사용자가 준 "학습 성향 진단해봤는데 소름... 너네 아이도 해봐" 톤을 그대로 기본값으로)를 수정 가능한 입력창으로 노출. 공유 버튼은 `navigator.share()`를 우선 사용 — 모바일 OS 공유 시트에 카카오톡이 대상으로 뜨므로, 별도 Kakao SDK 앱키 등록(Groble처럼 외부 서비스 설정이 필요) 없이도 "카카오톡 공유"를 사실상 만족한다. 미지원 환경(주로 데스크톱)은 클립보드 복사로 폴백(기존 동작 유지). `isSharedView`(공유 링크로 들어온 방문자)일 때는 "나도 우리 아이 진단해보기"를 `btn-primary`로 승격하고 "이 진단, 우리 아이는 어떨까요?" 리드인 문구를 추가, 반대로 공유 버튼은 `btn-secondary`로 낮춰 우선순위를 뒤집었다.
  - **한계 명시**: 카카오톡 "말풍선 카드" 같은 리치 프리뷰 공유는 Kakao JavaScript 키 발급·앱 등록이 필요해 이번엔 못 했다. 필요하면 `lib/groble.ts`와 같은 패턴(환경변수 미설정 시 기존 방식으로 폴백)으로 추가 가능.
- **검증**: `npx tsc --noEmit`, `check-scoring.ts`(옵션 2~5개, 채점 문항 전부 uncertain 보유, 보조문항 구조 검증, "잘 모르겠어요 2회→보조문항 전/후 답변수 3→4" 케이스 추가) 통과. 브라우저로 초등중학년+학원있음 경로에서 자율성 축 1·2번째 문항에 "잘 모르겠어요" 선택 → 3~5번째 일반 응답 → **보조 문항이 정확한 배너 문구로 삽입되는 것**, 답변 후 다음 블록(6/30, 학습 수준·격차 1/5)으로 정상 진행되는 것, 콘솔 에러 없는 것 확인. `/report/example`로 위젯이 넓어진 range에서도 안 깨지는 것 확인. `/api/report` 실제 호출(로딩 화면·공유 UI 최종 렌더)은 프로덕션 DB 쓰기 방지 원칙상 하지 않음 — 코드 리뷰 + tsc로 갈음.
- **영향**: `lib/questions.ts`(uncertain, SUPPLEMENTARY_QUESTIONS), `lib/scoring.ts`(ALL_SCORABLE_QUESTIONS, answeredCount), `lib/prompt.ts`(v12), `lib/llm-mock.ts`, `app/survey/page.tsx`, `app/components/report-loading.tsx`, `app/components/result-view.tsx`, `scripts/check-scoring.ts`.
- **바로 다음 세션에서 수정**: D-25 직후 사용자가 "공유 문구 입력창은 과했다"고 되돌리라 지시 — D-26에서 원래의 단순 "결과 공유하기" 버튼(고정 문구, `navigator.share()`/클립보드 로직은 유지)으로 되돌렸다.

## D-26 (2026-08-01) 무료/유료 분리 폐지 — 60문항(축당 12) 단일 플로우 + 리포트 열람 게이트
사용자가 구조를 최종 확정: 부분잠금(D-07) 대신 **전원 60문항**을 다 풀게 하고, 결제 선택은 **리포트를 열람하는 시점 하나**로 옮긴다. 착수 전 사용자 스펙의 산술 불일치(“5축×12=60”인데 “축마다 6개 추가”라 쓰여 있어 55가 됨, 학습스타일·강점 카테고리가 계산에서 빠져 있음)를 확인 질문으로 짚었고, 답변에 따라 **축당 12문항(신규 7개씩)** + **학습스타일·강점 5문항 그대로 유지(실질 총 65문항)**로 확정했다.

- **문항 확장**: 기존 25문항(축당 5)에 축당 7개씩 총 35문항을 추가해 채점 5축 × 12 = 60문항. 신규 문항은 "숙제만 반복하지 않기" 위해 놀이·친구·가족·처음 겪는 상황·실패 상황으로 장면을 다양화했고, 여러 개는 기존 문항과 명시적 "교차검증 쌍"으로 설계했다(예: q31↔q9 자발적 착수를 놀이/학업 두 장면에서, q56↔q4 실패 후 자기효능감 서술을 실패 상황/시험지 두 장면에서). 전부 D-25의 `uncertain` 옵션도 포함. `lib/questions.ts` 배열 순서는 축별로 연속 배치(자율성 1~12·수준격차 13~24·번아웃 25~36·유능감 37~48·학습스타일 49~53·사회성 54~65)해 아래 블록 경계 로직이 성립한다.
- **check-scoring.ts 자동화**: 65문항 전부를 손으로 나열하던 positive/strained 테스트 케이스를 `answersAt(pickIndex)` 헬퍼로 프로그램적으로 생성하도록 바꿨다(문항이 늘어날수록 수작업 나열은 비현실적). q3·q7처럼 옵션 순서가 예외인 legacy 문항이 섞여 있어도, 축당 12문항으로 늘어난 덕분에 기존 방향성 검증(>=70/<=30 등)은 전부 그대로 통과했다.
- **인터루드·보조문항 트리거 재계산**: `AXIS_BLOCK_ENDS`를 12문항 블록 기준(12/24/36/48/65)으로, `INTERLUDE_STEPS`를 10문항마다(10/20/30/40/50/60)로 재계산. 이전엔 두 지점이 우연히 겹쳤지만(step 10=zpd_strain 블록 끝) 이번엔 완전히 어긋난다 — `advance()`가 둘을 독립적으로 체크하는 기존 구조 덕분에 코드 변경 없이 자연스럽게 맞물린다.
- **응답 유실 방지**: `lib/survey-progress.ts` 신설. 매 문항 응답 직후(`advance()`, 보조문항 트리거 시점)에 `{childName, ageBand, hagwonStatus, answers, step, triggeredAxes}`를 localStorage에 저장(문항 수를 `version`으로 같이 저장해 문항 구조가 바뀌면 자동 폐기, 7일 지나도 폐기). 인트로 화면에 저장된 진행이 있으면 "이어서 하기/새로 시작" 배너를 보여준다. 제출 완료 시 삭제. sessionStorage(이름/나이대/학원여부, 완료된 답변)는 결과 화면 전달용으로 기존 역할 그대로 유지 — 이건 "진행 중" 상태를 브라우저를 꺼도 남기기 위한 별도 저장소다.
  - **한계**: 인터루드·보조문항 화면 "도중"에 이탈하면 그 화면은 복원하지 않고 다음 실제 문항으로 건너뛴다(트랜지언트 화면까지 복원하려면 상태 모델이 훨씬 복잡해져 범위를 좁혔다). "이전" 버튼으로 되돌아간 뒤 이탈하면 저장은 그 이전 시점이 아니라 마지막 `advance()` 시점 그대로 남는다 — 드문 경우라 허용.
- **리포트 열람 게이트**: `app/components/report-gate-screen.tsx` 신설(요청대로 별도 컴포넌트로 분리, 버튼만 나중에 결제 버튼으로 교체 가능). 로딩 완료 → "{이름}의 {N}개 응답 분석이 끝났어요" + 목차 미리보기(성향 유형/5축 레벨 해설/재해석 인사이트/학원 추천) + "상세 리포트 8,000원"(취소선) + "파일럿 기간 무료로 열람하기" 버튼 → 클릭해야 실제 리포트(`ReportView`)가 보인다. `initialReport`로 전달되는 경로(공유 링크 `/r/[code]`, 예시 페이지)는 게이트를 건너뛴다 — 이미 완성된 리포트를 보러 온 것이지 방금 60문항을 푼 게 아니므로.
  - **D-07 부분잠금과의 관계 (사용자 결정)**: 완전히 대체하지 않고 **그대로 남겨둔 채 비활성 상태 유지**. `PAYWALL_ENABLED`가 계속 꺼져 있어 `report.locked`가 항상 false라 기존 블러 UI는 지금 안 보인다. 나중에 그 스위치를 켜면 새 게이트(통과해야 리포트 진입) + 기존 부분잠금(리포트 안에서 "어쩌면 의외의 모습"부터 다시 블러)이 동시에 작동한다 — 이중 게이트가 되는 건 의도된 것(사용자가 "리포트 일부 블러처리하여 과금 유도"를 명시적으로 요청). 기존 잠금 CTA 카드가 새 게이트와 동시에 뜨지 않도록 `report.locked` 조건에 `gatePassed`를 추가했다.
- **화면 표기**: 인트로 "65문항 · 약 15~20분 · 5개 영역 12문항씩 교차 측정". 결과 화면 헤드라인 아래 "N개 응답 · 5개 영역 · 영역당 12문항 교차 측정" — N은 채점 5축의 `answeredCount` 합계를 실시간 계산(보통 60, "잘 모르겠어요"·보조문항에 따라 살짝 달라질 수 있음. D-25에서 확립한 "하드코딩 대신 실제값" 원칙을 그대로 따름).
- **공유 문구 입력창 되돌림**: D-25에서 추가한 "공유 문구 수정 가능" 입력창을 이번에 제거해 달라는 요청 — 원래의 단순한 "결과 공유하기" 버튼 하나로 복귀. `navigator.share()`/클립보드 폴백 로직 자체는 유지, 문구만 고정 문자열로.
- **검증**: `npx tsc --noEmit`, `check-scoring.ts`(65문항, 카테고리별 12/12/12/12/12/5, 보조문항 구조, 잘모르겠어요→보조문항 답변수 10→11 케이스) 통과. 브라우저로 **실제 65문항 전체를 완주**하며 확인: 인트로 "65문항" 표기 → 4문항 응답 후 새로고침 → "이어서 하기" 배너 → 클릭 시 정확히 5/65로 복원 → 축 라벨"n/12" 정상 증가 → 학습스타일 블록 "5/5" → 65/65 제출 → 결과 화면 "60개 응답·5개 영역·12문항" 표기 → 로딩 3단계 애니메이션 → **게이트 화면**(목차·8,000원·파일럿 버튼) → 클릭 시 실제 Gemini 리포트(신규 문항 내용이 자연스럽게 인용됨) 정상 렌더. 전 구간 콘솔 에러 없음. 테스트로 생성된 진단은 확인 후 즉시 삭제.
- **알려진 별개 이슈(이번엔 미수정)**: 로컬 개발 모드에서 `/api/report`가 진단 1건당 2회 호출되는 현상을 이번에도 재확인(React 18 StrictMode의 effect 이중 실행으로 추정, 프로덕션 빌드엔 영향 없을 가능성이 높음). 근본 수정(예: `AbortController`로 실제 fetch를 취소하거나 요청 단위 idempotency key 부여)은 범위 밖이라 보류 — 다음에 다룰 것.
- **영향**: `lib/questions.ts`(35문항 추가), `scripts/check-scoring.ts`, `app/survey/page.tsx`(블록 경계·인터루드 상수, 응답 저장), `lib/survey-progress.ts`(신설), `app/components/report-gate-screen.tsx`(신설), `app/components/result-view.tsx`(게이트 연결, 공유 버튼 단순화, 상단 표기).

## D-27 (2026-08-03) 축 숫자의 "방향" 안내 — 역방향 2축 오해 차단 (프롬프트 v13)
사용자 피드백: 정서·번아웃이 레벨 2로 나온 학부모가 그 숫자를 "5점 만점에 2점"으로 읽어 오해했다. 확인해 보니 문제는 번아웃 하나가 아니라 **5축 중 2축(`burnout`, `zpd_strain`)이 "높을수록 좋음" 직관과 반대 방향**인데, UI는 5축을 전부 같은 문법("레벨 X / 5" + 채워지는 게이지 + 넓어지는 오각형)으로 그리고 있었다는 것 — 셋 다 "클수록 좋다"로 읽히는 표현이다.

- **`AXIS_SCALE_NOTE`(`lib/questions.ts` 신설)**: 5축 × "숫자가 커진다는 게 무슨 뜻인지" 한 문장. 숫자가 보이는 모든 자리에 함께 표시한다 — D-24가 `LEVEL_MEANING`으로 "이 레벨이 어떤 모습인지"를 채웠다면, 이건 "숫자가 어느 쪽으로 커지는지"로 역할이 다르다. **역방향 2축에만 캡션을 달지 않고 5축 전부에 둔 이유**: 둘만 표시하면 그 둘이 "나쁜 축"으로 읽혀 D-10 반전 프레이밍이 깨진다. 문장은 좋고 나쁨을 말하지 않고 무엇이 커지는지만 말한다.
- **`STRAIN_AXES`(신설)** = `['zpd_strain', 'burnout']`. "숫자가 클수록 지금 부담이 크다"는 축. 게이지·레이더 꼭짓점·OG 카드에서 이 두 축은 성취색(amber/sage) 대신 중립색(`--navy-muted`)을 쓴다 — 바가 꽉 차거나 오각형이 넓은 게 좋은 결과가 아니기 때문.
- **레이더 차트**: D-24의 "숫자 나오는 곳 전수 조사"는 레벨 표기만 대상이라 레이더를 제외했고, *방향* 문제는 그때 다뤄진 적이 없었다. 이번에 차트 아래 "회색 점(정서·번아웃, 학습 수준·격차)은 숫자가 높을수록 지금 부담이 크다는 뜻이에요. 오각형이 넓다고 좋은 결과인 건 아니에요." 캡션을 추가. 폴리곤은 단일 path라 부분 색칠이 안 되므로 꼭짓점 색 + 캡션 조합으로 해결.
- **OG 이미지**: 공유 카드는 맥락이 전혀 없는 채로 `정서·번아웃 4 / 5`를 **초록색(sage)**으로 보여주고 있었다 — 받은 사람은 "4점, 좋네"로 읽는다. 역방향 축은 중립색으로 바꾸고 방향 캡션 한 줄 추가. 캡션 글자는 폰트 서브셋(`allText`)에 포함시켜야 렌더된다.
- **LLM에도 방향 명시(v13)**: `describeScores()`가 축 줄 괄호 안에 `AXIS_SCALE_NOTE`를 함께 넘기고, 시스템 프롬프트에 "이 두 축은 나머지와 방향이 반대이며 높은 점수를 좋은 결과로 서술하면 안 된다"는 지침을 추가. 기존엔 축별 지침 한 줄에 암시만 돼 있어 LLM이 방향을 추론에 의존하고 있었다.
- **선택하지 않은 대안**: 두 축을 '정서 에너지'·'난이도 적정성'처럼 **높을수록 좋은 방향으로 뒤집어 표시**하는 안. 한눈에는 가장 직관적이지만 ① `LEVEL_MEANING` 25문장·프롬프트·`AXIS_NARRATIVE`가 전부 어긋나고 ② 이미 DB `content_md`에 저장된 리포트 본문("정서·번아웃이 높은 편")과 화면이 모순돼 **기존 공유 링크가 깨진 설명을 보여주게 된다**(D-07 각주와 같은 계열의 함정). 게다가 "오각형이 클수록 좋다"는 읽기를 오히려 공식화하는 셈이라 D-10과도 충돌. 값·라벨은 그대로 두고 방향 안내만 붙이는 쪽을 택했다.
- **검증**: `npx tsc --noEmit`, `check-scoring.ts` 통과. `/report/example`(고정 리포트라 `/api/report` 호출 없음)에서 5축 모두 방향 문장이 레벨 배지 아래 렌더되는 것, 게이지 색이 amber 3축 / navy-muted 2축으로 갈리는 것, 레이더 꼭짓점도 같은 규칙으로 칠해지는 것을 `getComputedStyle`로 확인(`rgb(200,134,10)` × 3, `rgb(107,127,151)` × 2). OG 이미지는 무상태 코드(`'1'×65`)로 실제 라우트를 호출해 200 + Satori 예외 없음 확인(DB 읽기만, 행 생성 없음). 콘솔 에러 없음.
- **카피 수정**: 축 라벨 자체에 가운뎃점이 들어 있어("정서·번아웃") 축을 `·`로 이어붙이면 한 덩어리로 읽힌다 → 구분자는 쉼표. 조사도 라벨 뒤에 직접 붙이지 않는다(`격차는`/`번아웃은`이 축 순서에 따라 달라짐) — "회색 점(…)은", "… 두 축은"처럼 조사가 붙는 명사를 따로 둔다.
- **영향**: `lib/questions.ts`(AXIS_SCALE_NOTE, STRAIN_AXES), `lib/scoring.ts`(describeScores), `lib/prompt.ts`(v13), `lib/llm-mock.ts`, `app/components/result-view.tsx`, `app/components/radar-chart.tsx`, `app/r/[code]/opengraph-image.tsx`, `app/globals.css`(.axis-fill-strain).

## D-28 (2026-08-03) 히어로 재작성 — 말풍선 + shimmer, 그리고 걸러낸 마케팅 문구
새 마케팅 카피를 히어로에 반영. 기존 헤드라인("우리 아이, 어떻게 공부할 때 가장 빛날까요?")·서브 문구·SDT/ZPD 설명 박스를 걷어내고 **학부모의 속마음을 그대로 옮긴 말풍선**을 히어로의 중심에 뒀다. 광고 카피가 아니라 "내 얘기"로 읽히게 하는 게 목적.

- **말풍선(`.hero-bubble`)**: 아이보리 배경 위 흰 카드(radius 22px, 그림자로 떠 있는 느낌), 꼬리는 하단 왼쪽. 꼬리는 **테두리색 삼각형 위에 배경색 삼각형을 2px 겹쳐** 테두리 선이 꼬리까지 이어져 보이게 했다.
- **shimmer를 `background-image`로 구현한 이유**: `::after`에 빛 띠를 만들면 `overflow: hidden`으로 클리핑해야 하는데, 그러면 **꼬리(`::before`/`::after`)가 잘려 나간다.** 배경은 `border-radius`로 자동 클리핑되므로 `background-position` 애니메이션이면 꼬리를 살린 채 shimmer를 줄 수 있다. 4.5s 중 앞 72%는 화면 밖 대기 → 후반 1.3s에 한 번만 스친다(요청한 "3~4초 간격으로 한 번").
- **반짝이**: `clip-path` 4각 별 3개, 지속시간·딜레이를 각각 다르게(3.4/4.3/3.9s, 0/1.1/2.2s). **전부 말풍선 가장자리 바깥에 배치** — 본문 위에 겹치면 가독성을 해치기 때문. 최대 불투명도 0.85, 기본 0.16으로 은은하게.
- **`prefers-reduced-motion`**: 저장소에 이 미디어쿼리가 아예 없었다. 이번에 전역 블록을 추가해 신규 애니메이션뿐 아니라 기존 `free-badge-glow`·`loading-dots`·`progress-shimmer`까지 함께 끈다. 말풍선은 shimmer 그라디언트 자체를 제거하고, 반짝이는 정적 opacity로 고정.
- **반영하지 않은 카피 2건** (사용자 확인 후 결정):
  - **"최신 학회 연구 반영 업데이트" → 뺐다.** 저장소 어디에도 근거가 없고, 근거는 SDT·ZPD·학업 소진 연구라는 *확립된* 이론이지 최신 연구가 아니다. 더구나 T-14에서 이보다 약한 '교육심리학 기반' 배지조차 "과한 강조"로 뺀 전례가 있어 그 절제선을 넘는다. "교육심리학 이론 기반"으로 대체.
  - **"60문항" → "65문항".** 60은 채점 축 문항 수(5영역×12)라 "교차 측정" 맥락에선 맞지만, 사용자가 실제로 답하는 개수는 65다(D-26). 설문 인트로 표기와 어긋나므로 65로 통일.
- **"커피 두 잔 값" vs 무료 배지**: 히어로 최상단 `isFree` 배지와 정면으로 충돌했다. 배지 문구를 "지금은 무료 진단 서비스 중" → **"파일럿 기간 · 지금은 무료"**로 바꿔 "원래 커피 두 잔 값 → 지금은 파일럿이라 무료"로 읽히게 했다. 게이트 화면의 `8,000원` 취소선 처리와도 일관된다. 유료 전환 시 배지만 내리면 가격 문구는 그대로 살아 있다.
- **검증**: `npx tsc --noEmit` 통과. 375px에서 가로 스크롤 없음(`scrollWidth === innerWidth === 375`), 말풍선이 wrap 패딩 안에 들어옴(20~355.2px), 하드 `<br/>` 3줄이 추가 줄바꿈 없이 정확히 3줄(`textLines: 3`), 반짝이 3개 모두 뷰포트 안. 600px에서 폰트 19px 분기 정상, 역시 3줄·오버플로 없음. 꼬리 좌표(outer `-13px`/inner `-11px`, 둘 다 `left: 28px`)와 애니메이션(`hero-shimmer 4.5s ease-in-out infinite`, 반짝이 3종 각기 다른 duration/delay), `prefers-reduced-motion` 블록 파싱을 `getComputedStyle`·`document.styleSheets`로 확인. 콘솔 에러 없음. **스크린샷은 이 환경에서 Browser pane이 표시되지 않아 캡처하지 못했다** — 시각 확인은 사용자가 직접 필요.
- **후속 조정 (같은 날)**: 사용자가 말풍선이 너무 크다고 판단 — 크기를 약 1/3로 줄이고 테두리를 글로우로 바꿨다. 폭을 줄이려면 `display: inline-block`이 필요하다(블록이면 항상 전폭이라 "작은 속마음"으로 안 읽힘). 글자 12/13px, 패딩·꼬리·반짝이도 같은 비율로 축소, radius 22→14px. **실측 면적비는 모바일 39%·데스크톱 27%** — 정확히 1/3까지 줄이려면 글자를 11px로 내려야 하는데 한글 세리프 가독성 하한이라 12px에서 멈췄다. 테두리는 앰버 링(`box-shadow` spread만 키워 레이아웃 영향 없음)이 3.4s로 커졌다 작아지는 `hero-bubble-glow`. **애니메이션이 꺼진 상태에서도 글로우가 남도록 `.hero-bubble`에 정적 `box-shadow`를 따로 둔다** — 키프레임에만 두면 모션 최소화 시 `animation-fill-mode: none` 때문에 그림자가 통째로 사라진다. 말풍선이 작아지면서 히어로의 시각적 앵커가 없어져 **`h1`을 "커피 두 잔 값…" 문구로 옮기고**(20/23px 세리프) 말풍선 본문은 `<p>`로 내렸다 — 12px 텍스트가 `h1`이고 그 아래 더 큰 문구가 오는 구조는 SEO·접근성 모두 어색하다.
- **영향**: `app/page.tsx`(히어로 전체), `app/globals.css`(.hero-bubble/.hero-sparkle/.hero-sub/.hero-trust, hero-bubble-glow, prefers-reduced-motion 전역 블록).

## D-29 (2026-08-03) 인터루드 문구 반복 버그 수정 — step 고정 방식으로 전환
사용자 피드백: 인터루드(10문항마다 나오는 "지금까지의 응답에서 이런 모습이…" 카드)가 "혼자보다 함께일 때 힘을 내는, 신뢰 속에서 배우는 아이예요" 한 문장만 계속 나온다.

- **원인**: `pickInterludeAxis()`가 "50점 기준 편차가 가장 큰 축"을 골랐는데, 문항이 축별 12개씩 블록으로 묶여 있어(D-26) 초반엔 첫 블록(자율성)만 값이 다 채워지고 나머지 축은 미응답분이 중앙값(사실상 0)으로 남는다. 그래서 편차 1위가 6번의 인터루드 내내 계속 자율성으로 뽑혔다 — "반응성이 없다"는 인상의 원인.
- **수정**: `INTERLUDE_BLOCK`(step→그 시점 문항 블록)으로 교체 — 10/20/30/40/60은 자율성/학습 수준·격차/정서·번아웃/유능감/관계·사회성, 50은 학습스타일·강점(`style_strength`, `AXIS_BLOCK_ENDS`와 동일 매핑, D-26). "방금 막 답한 블록의 이야기"가 매번 나온다.
- **방향 판정도 바꿨다**: 정규화 점수(0~100) 대신 **raw 합의 부호**를 쓴다. 정규화는 이론적 min/max 기준이라 부분 응답 상태에서는 실제로 답한 문항의 신호와 반대로 나올 수 있다(예: 12문항 중 절반만 긍정 방향으로 답해도 정규화가 아직 50 밑일 수 있음) — raw는 "지금까지 고른 선택지들의 순수 합"이라 그 블록만 놓고 볼 때 더 정확하다.
- **style_strength(step 50) 별도 버그**: 이 시점엔 보통 style 문항(q6·q16·q30)만 답한 상태이고 focus 문항(q8·q17)은 아직 안 나온 상태다. `focus`는 최빈값 계산이라 응답이 하나도 없어도 폴백값('유연한')이 그냥 나와, 실제로 안 물어본 성향을 보여주는 셈이었다. `partialAnswers`에 `focus`가 달린 옵션이 실제로 하나라도 있는지 확인해, 없으면 "OO 방식이 편해 보여요"로 짧게 줄인다.
- **카피**: "지금까지의 응답에서 이런 모습이 보이기 시작했어요"(15px, 강조) → **"지금까지의 응답에서 이런 모습이 보입니다"(12px, `--navy-muted`)**로 낮추고, 성향 문장을 15px·굵게·`--navy`로 올렸다(사용자 요청 — 리드인은 거들 뿐, 성향 문장이 주인공).
- **검증**: `npx tsc --noEmit` 통과. 임시 스크립트(커밋 없음)로 ① `INTERLUDE_BLOCK`의 6개 step이 실제 그 시점 마지막 문항의 `category`와 정확히 일치하는지, ② 4가지 응답 패턴(전부 최상단/전부 최하단/번갈아/전부 두 번째)에서 6회 인터루드 멘트가 **매번 서로 다른지** 확인 — 4패턴 모두 6/6 고유. 브라우저로 실제 10문항→20문항 두 인터루드를 완주해 각각 자율성·학습 수준·격차 멘트가 정상 출력되는 것, 리드인 12px/성향 문장 15px 폰트 크기 차이를 `getComputedStyle`로 확인. 콘솔 에러 없음.
- **영향**: `app/survey/page.tsx`(`pickInterludeAxis` 삭제 → `INTERLUDE_BLOCK`·`interludeMessage` 신설, 인터루드 카드 마크업).
